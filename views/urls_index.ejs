<!DOCTYPE html>
<html lang='en'>
<head>
  <% include ./partials/_header %>
</head>
<body>
  <% if (userID) { %>

  <h2>URL List</h2>
  <p> <% for (let shortUrl in urls) { %></p>
  <p><strong><%= shortUrl %></strong> is the id for: <%= urls[shortUrl].url %> <a href="/urls/<%= shortUrl %>/">Edit</a></p>
  <p>
    <form method="POST" action='/urls/<%= shortUrl %>/delete'>
      <input type='submit' value='Delete!'>
    </form>
    <% } %>
  </p>
  <p> <a href='/urls/new'>Create a new short url </a> </p>
  <% } else { %>
    <h2><a href="/login">Login</a> to access your URL List</h2>
  <% } %>
</body>
</html>
